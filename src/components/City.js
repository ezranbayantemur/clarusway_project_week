import React from 'react';
import {TouchableOpacity, Text} from 'react-native';
import { cityStyle } from '../styles';

const City = (props) => {
  return (
    // City button..
    null
  );
};

export {City};
